import React, { FC } from 'react';
import AppContainer from '../../../ui/AppLayout/AppContainer/AppContainer';

const FormModel: FC = () => {
  return (
    <AppContainer>
      <h2>Сверстать форму оформления заказа на аренду авто (шаг 2)</h2>
    </AppContainer>
  );
};

export default FormModel;
