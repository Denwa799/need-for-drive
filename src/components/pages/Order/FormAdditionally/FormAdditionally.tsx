import React, { FC } from 'react';
import AppContainer from '../../../ui/AppLayout/AppContainer/AppContainer';

const FormAdditionally: FC = () => {
  return (
    <AppContainer>
      <h2>Форма выбора дополнительных параметров</h2>
    </AppContainer>
  );
};

export default FormAdditionally;
