import React, { FC } from 'react';
import AppContainer from '../../../ui/AppLayout/AppContainer/AppContainer';

const FormModel: FC = () => {
  return (
    <AppContainer>
      <h2>Форма выбора модели машины</h2>
    </AppContainer>
  );
};

export default FormModel;
